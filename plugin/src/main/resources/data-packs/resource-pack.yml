pack:
  enabled: true
  description: "STEMCraft"
  # The maximum is derived from the running Minecraft version (88 for 26.2).
  # Raise this only when older Java clients no longer need the same pack.
  min_pack_format: 32
