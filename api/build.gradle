plugins {
    id 'java-library'
    id 'maven-publish'
}

apply from: "$rootDir/buildSource.gradle"

def apiMavenRepoDir = (findProperty("stemcraftApiRepoDir")
    ?: System.getenv("MAVEN_REPO_DIR")
    ?: "/repos/maven/maven-public").toString()

repositories {
    mavenCentral()
    maven { url = uri("https://repo.papermc.io/repository/maven-public/") }
    maven { url = uri("https://repo.opencollab.dev/main/") }
    maven { url = uri("https://maven.enginehub.org/repo/") }
}

dependencies {
    //noinspection VulnerableLibrariesLocal
    compileOnly("io.papermc.paper:paper-api:${rootProject.ext.paperApiVersion}")

    // WorldEdit
    compileOnly("com.sk89q.worldedit:worldedit-bukkit:7.4.4")

    compileOnly("com.google.code.findbugs:jsr305:3.0.2")

    // GeyserMC API
    compileOnly('org.geysermc.geyser:api:2.9.0-SNAPSHOT')

    // LuckPerms
    compileOnly 'net.luckperms:api:5.4'

    // Lombok
    compileOnly "org.projectlombok:lombok:1.18.42"
    annotationProcessor "org.projectlombok:lombok:1.18.42"

    testImplementation "org.junit.jupiter:junit-jupiter:5.11.0"
    testImplementation "org.mockito:mockito-core:5.23.0"
    //noinspection VulnerableLibrariesLocal
    testImplementation "io.papermc.paper:paper-api:${rootProject.ext.paperApiVersion}"
    //noinspection VulnerableLibrariesLocal
    testImplementation("org.mockbukkit.mockbukkit:mockbukkit-v26.1.2:4.114.0") {
        exclude group: "org.junit"
        exclude group: "org.junit.jupiter"
        exclude group: "org.junit.platform"
    }
    testImplementation("com.google.code.gson:gson:2.11.0")
    testImplementation("com.google.code.findbugs:jsr305:3.0.2")
    testImplementation("org.geysermc.geyser:api:2.9.0-SNAPSHOT")
    testImplementation("net.luckperms:api:5.4")
    testImplementation("com.sk89q.worldedit:worldedit-bukkit:7.4.4")
    testRuntimeOnly "org.junit.platform:junit-platform-launcher:1.11.0"
}

test {
    useJUnitPlatform()
}
publishing {
    publications {
        create("mavenJava", MavenPublication) {
            from components.java
            artifactId = "stemcraft-api"
            version = project.version.toString()
        }
    }
    repositories {
        maven { url = uri(new File(apiMavenRepoDir)) }
    }
}

tasks.named("jar", Jar) {
    archiveBaseName.set("STEMCraft-API")

    archiveVersion.set("${project.version}${getArchiveBuildMetadata()}")
}
