# Resource Pack

Allow the STEMCraft server resource pack when connecting. It provides the models, icons, fonts, and interface artwork used by custom items and server messages.

If custom items appear as ordinary items or interface symbols are missing, reconnect and accept the pack. Java players can also use `/resourcepack` to ask the server to send it again.
